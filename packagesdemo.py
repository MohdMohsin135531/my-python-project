import pandas as pd # importing pandas package. There are many packages in Python like text to speech, speech recognition, machine learning, data analysis, etc. We can install these packages using pip install package_name command in the terminal. After installing the package, we can import it in our Python code using the import statement. 
 
df = pd.read_excel("file.xlsx")
print(df) 